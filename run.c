#include <stdio.h>
#define PORT 3000
int verify(){
    system(node -v & npm -v);
};
int startpro(){
char url[20]='https://localhost:3000';
    system(node server & start url);
    printf('Server started Successfully and App opend Successfully\n');
};
int main(){
int verify() startpro();
printf('success');
    return 0;
}